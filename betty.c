#include <stdio.h>

int main(void) {
    int i = 42;  // Declare and initialize a variable

    /* Print a simple message to demonstrate functionality */
    printf("The value of i is: %d\n", i);

    return 0;  // Indicate successful program termination
}

